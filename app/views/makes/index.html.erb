<head>
  <style>
    body {background-color: peachpuff;}
    h1   {color: blue;font-family:verdana;color:maroon;font-size:40px;}
    p    {color: red;}
    th {color:maroon;colspan:3;}
    tr {font-size:20px;}
    col {width:130;}
  </style>
</head>

<p id="notice"><%= notice %></p>

<%= form_tag(search_makes_url, method: "get") do %>
  <%= label_tag(:search, "Search for: (Can search for 'Name' or 'Country')") %>
  <%= text_field_tag :search, params[:search], data: { autocomplete: autocomplete_make_name_makes_path } %>
  <%= submit_tag("Search") %>
<% end %>
<p>Autocomplete works when searching for make names.</p>

<%= link_to 'Log out', destroy_user_session_path, method: :delete %>

<h1>Makes</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Country</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @makes.each do |make| %>
      <tr>
        <td><%= make.name %></td>
        <td><%= make.country %></td>
        <td><%= link_to 'Show', make %></td>
        <td><%= link_to 'Edit', edit_make_path(make) %></td>
        <td><%= link_to 'Destroy', make, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Make', new_make_path %>
