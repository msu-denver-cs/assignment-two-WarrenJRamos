<%= form_with(model: car, local: true) do |form| %>
  <% if car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(car.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
      <% car.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!-- MIGHT WANT TO ADD THIS BACK IN LATER
  <div class="field">
    <%= form.label :make_title %>
    <%= form.text_field :make_title %>
  </div>
  -->
  
  <div class="field">
    <%= form.label :make_id %>
    <%= form.collection_select :make_id, Make.all, :id, :name %>
  </div>

  <div class="field">
    <%= form.label :model_title %>
    <%= form.text_field :model_title %>
  </div>

  <div class="field">
    <%= form.label :vin_number %>
    <%= form.number_field :vin_number %>
  </div>

  <!--
  <div class="field">
    <%= form.label :part_id %>
    <%= form.collection_select :part_id, Part.all, :id, :part_name %>
  </div>
  -->

  <div class="field">
    <%= form.label :car_parts %><br>
    <%= form.collection_check_boxes(:part_ids, @parts, :id, :part_name) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
