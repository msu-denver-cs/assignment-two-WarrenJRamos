<p id="notice"><%= notice %></p>

<head>
  <style>
    body {background-color: peachpuff;}
    h1   {color: blue;font-family:verdana;color:maroon;font-size:40px;}
    p    {color: red;}
    th {color:maroon;colspan:3;}
    tr {font-size:20px;}
    col {width:130;}
  </style>
</head>

<%= form_tag(search_parts_url, method: "get") do %>
  <%= label_tag(:search, "Search for:") %>
  <%= text_field_tag :search, params[:search], data: { autocomplete: autocomplete_part_part_name_parts_path } %>
  <%= submit_tag("Search") %>
<% end %>
<p>Autocomplete works for searching here.</p>

<%= link_to 'Log out', destroy_user_session_path, method: :delete %>

<h1>Parts</h1>

<table>
  <thead>
    <tr>
      <th>Part name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @parts.each do |part| %>
      <tr>
        <td><%= part.part_name %></td>
        <td><%= link_to 'Show', part %></td>
        <td><%= link_to 'Edit', edit_part_path(part) %></td>
        <td><%= link_to 'Destroy', part, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Part', new_part_path %>
